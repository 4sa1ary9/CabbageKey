# KeyVault

本地、单机的个人 API key 收纳工具：无密码、无加密，全部记录以明文存于单个 vault 文件，拷贝整个文件夹即可迁移到另一台机器。不做云端服务、不做多人共享、不做全功能密码管理器，也不依赖网盘同步——这是"简单"的准确含义（指产品形态，不指功能数量）。

## Language

**Vault（保险库）**:
单个明文 JSON 数据文件，包含全部记录。无密码、无加密；拷贝文件即完成迁移。
_Avoid_: 数据库、db

**记录（Record）**:
一条 API key 收纳条目。必填：用途名称、api_key；可选：厂商、端点 URL（按接口规范分别保存）、接口规范、官网、备注、标签。
_Avoid_: 条目、账号

**记录顺序（Record Order）**:
记录在列表中的显示顺序，由用户拖拽自定义并保存在 vault 中；旧 vault 无此数据时按文件内原有顺序。
_Avoid_: 排序、位置

**厂商预设（Vendor Preset）**:
内置的 AI 厂商模板。选中后自动填充端点 URL、官网与支持的接口规范。编辑记录时，若厂商匹配预设，缺失的端点 URL 会自动补全（不覆盖已有值）；用户主动切换厂商时全量替换。
_Avoid_: 模板

**自定义厂商（Custom Vendor）**:
不在厂商预设列表中的厂商名，由用户自由输入（可能来自旧数据）。界面始终显示其真实名称，而不是"自定义"。
_Avoid_: 自定义（作为厂商名时）、其他厂商

**接口规范（API Standard）**:
一条记录所声明的 API 协议形态，恰为四种之一：OpenAI Chat Completions、OpenAI Responses API、Anthropic Messages、Gemini generateContent。一条记录可为任一规范配置端点 URL；URL 为空（未配置）的规范仍被记录，界面中显示为置灰。
_Avoid_: 协议、标准

**迁移（Migration）**:
把 vault 文件（或整个数据文件夹）从一台机器拷贝到另一台机器即可继续使用。无密钥、无密码、无账号依赖。
_Avoid_: 同步、网盘同步

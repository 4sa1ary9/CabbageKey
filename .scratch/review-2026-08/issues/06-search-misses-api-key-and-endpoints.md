# 06 — 搜索不覆盖 api_key 与端点 URL

> *This was generated by AI during triage.*

**Category:** enhancement

**Status:** done

## Triage Notes

**现象：**
关键字搜索只匹配 用途名称 / 厂商 / 备注 / 标签；api_key 与端点 URL 不参与。DESIGN.md 定义的三条找回路径（按厂商 / 按用途 / 按关键字）里，"只记得 key 的一段"目前无处安放——而"拿着一段 key 反查是哪条记录"正是这类收纳工具的典型场景。本地明文存储，搜索无隐私或性能障碍。

**建议方案：**
把 api_key 与全部端点 URL 值加进搜索 haystack（与现有字段同权拼接即可）。命中时列表只显示"该记录匹配"，不显示 key 片段本身，无泄露面变化。

**待维护者定夺：**
- 是否接受"key 片段可被搜索"（家用场景应当无异议，但属产品决策）
- 优先级（相对 01–05 的 bug 修复）

## Comments

> *This was generated by AI during triage.*

- 2026-08-29 实现完成（按票内建议）：搜索 haystack 加入 api_key 与全部端点 URL 值，与既有字段同权拼接；命中仍只显示"记录匹配"，不暴露 key 片段。新增 api_key 片段与 URL 域名匹配单测。vitest 94/94。

# 13 — 对话框交互打磨（Enter 提交 / 误关丢弃 / 复制反馈双份）

> *This was generated by AI during triage.*

**Category:** enhancement

**Status:** done

## Triage Notes

三个相关但独立的小交互问题，打包一票便于一起评估：

1. **厂商字段回车直接提交整个表单。** 厂商下拉关闭时，在表单第一个字段按 Enter 会走原生表单提交——用户只是想确认厂商，却得到"用途名称不能为空"的校验错误。常见组合框语义是 Enter 确认值并跳下一字段。
2. **误关无确认。** 点击遮罩或 Esc 会立即关闭对话框，已填内容直接丢弃且无确认。填写成本越高，这个越疼。
3. **复制反馈双份。** 复制成功时按钮变"✓ 已复制"（1.5s）+ toast 提示（2.5s）同时出现，信息重复；留一处即可（倾向留按钮态，toast 文案改为聚焦"30 秒后自动清空"这一个独有信息）。

**待维护者定夺：**
- 三项各做与否（可拆票）
- 第 2 项若做：确认弹层形态，或仅在有未保存修改时才拦截

## Comments

> *This was generated by AI during triage.*

- 2026-08-29 实现完成（三项全做）：① 厂商字段在下拉关闭时按 Enter 改为确认值并聚焦"用途名称"，不再提交表单；② backdrop/Esc 关闭前对比打开时的字段快照，有未保存修改先 `ask` 确认（显式"取消"按钮仍直接关闭）；③ 复制反馈留按钮态"✓ 已复制"，toast 改为只提示"剪贴板将在 30 秒后自动清空"。`vite build` 通过。
- 待真机冒烟：填写一半后按 Esc / 点 backdrop，确认确认框出现且"继续编辑"不丢内容。

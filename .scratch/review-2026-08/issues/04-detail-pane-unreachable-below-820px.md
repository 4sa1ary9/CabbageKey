# 04 — 窗口 720–820px 宽度区间详情面板不可达

> *This was generated by AI during triage.*

**Category:** bug

**Blocked by:** None — can start immediately

**Status:** done

## Agent Brief

**Category:** bug
**Summary:** 窄窗口下允许选中记录却看不到详情面板，需消除这个交互死路。

**Current behavior:**
窗口允许缩到 720px 宽（tauri.conf.json 的 minWidth），但样式在 ≤820px 时把详情栏 display:none（三栏变两栏）。在该区间用户可以搜索、选中记录，却完全看不到详情——包括复制 api_key、编辑、删除入口，没有任何替代呈现或提示。

**Desired behavior:**
以下任一方案均可，由实现者按改动量取舍：
(a) 把窗口 minWidth 提到 ≥820px，保证详情栏始终存在；或
(b) 保留 720px 下限，但窄区间给出详情的替代呈现（如选中后详情以浮层/抽屉出现，或至少给出"加宽窗口查看详情"的明确提示）。
不允许维持现状（可选中、无反馈、无出口）。

**Key interfaces:**
- tauri.conf.json 窗口 minWidth（方案 a）。
- 样式表中详情栏的窄屏媒体查询（方案 b）。

**Acceptance criteria:**
- [ ] 窗口缩到最小允许宽度时，选中任意记录都能看到其详情（或明确的替代呈现/提示）
- [ ] 复制 api_key / 编辑 / 删除在最小窗口下均可达
- [x] 默认宽度（1000px）下三栏布局不受影响
- [x] `bun run build` 通过

**Out of scope:**
- 移动端/响应式断点的整体重设计

## Comments

> *This was generated by AI during triage.*

- 2026-08-29 已实现（方案 a）：窗口 minWidth 720→860（>820 断点），三栏布局恒在、详情面板不可能再被隐藏；≤820px 的窄屏媒体查询随之不可达，已从样式中删除。vitest 94/94、`vite build` 通过。
- 待真机冒烟（验收项 1–2）：窗口缩到最小确认详情与复制/编辑/删除均可用。

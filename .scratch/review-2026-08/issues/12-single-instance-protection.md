# 12 — 无单实例保护，同 vault 双开 last-write-wins

> *This was generated by AI during triage.*

**Category:** enhancement

**Status:** done

## Triage Notes

**现象：**
应用可以同时开多个实例；会话态是"整个 vault 在内存、每次改动整文件覆写"，两个实例打开同一 vault 时后保存者整文件覆盖先保存者的全部改动，无提示、无冲突检测——明文化重构特意删掉了冲突检测（ADR-0002 语境），但"双开同一文件"是比网盘同步常见得多的本地场景（任务栏双击两次）。

**建议方案（任选其一）：**
a. 引入 Tauri 单实例插件：二次启动时聚焦已有窗口，直接消灭双开；
b. 保留多实例，写盘前校验文件 mtime/内容自读入后未被外部修改，变了则拒绝写并提示重开。

方案 a 更省事且贴合单机自用定位。

**待维护者定夺：**
- 方案 a / b / 明确接受双开风险（若接受，本票转 wontfix 并写入 .out-of-scope/）

## Comments

> *This was generated by AI during triage.*

- 2026-08-29 实现完成（方案 a）：接入 `tauri-plugin-single-instance`，二次启动聚焦已有 main 窗口，不再出现双实例写同一 vault。cargo test 30/30。
- 待真机冒烟：双击两次 exe 确认只开一个窗口且第二次聚焦已有窗口。

# 04 — 记录顺序数据模型

**What to build:** 后端支持"记录顺序（Record Order）"：每条记录新增顺序字段；新增/更新记录时顺序语义正确（新记录排在最后）；提供重新排序的 Tauri 命令（接收有序 id 列表，一次写入）；读取旧 vault（无顺序字段）时按文件内原有顺序兜底，不破坏数据。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 旧 vault 文件可正常读取，顺序字段缺失时保持文件内原顺序（迁移/默认逻辑有单测）
- [x] 重新排序命令：按传入的有序 id 列表更新全部记录顺序，成功返回最新视图；无效 id 报错不落盘
- [x] 新增记录默认排到最后；编辑记录不改变其顺序
- [x] 序列化输出确定（不影响既有 golden/字节级测试，如有则同步更新）
- [x] `cargo test` 通过

## Comments

> *This was generated by AI during triage.*

- 2026-08-29 triage：实现在主干（commit 23f78dd）：`Record.order` + `normalize_orders`（旧 vault 按文件位置兜底、显式值保留，有单测）、`reorder`（全量排列校验、非法输入不落盘，有单测）、新增记录 order = max+1、update 不改顺序；golden test 已含 order 字段冻结磁盘格式。复核 cargo test 29/29。`Status` 补记为 done，验收项勾选。

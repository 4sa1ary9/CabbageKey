# 01 — 详情页展示整改

**What to build:** 详情面板的信息顺序改为"厂商 → 官网 → api_key → 支持的接口规范 → 端点 URL → 标签 → 备注"（厂商/官网置顶，字段缺失时仍按现状省略）；api_key 遮盖串从 12 个 `•` 改为 24 个 `•`（显示与隐藏两处一致）。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 详情页字段顺序为新顺序（厂商在最上，官网次之，然后 api_key）
- [x] 无厂商/无官网的记录不显示对应字段（现状行为保留）
- [x] api_key 遮盖态显示 24 个 `•`，点击"显示"后正常露出真实值，再点"隐藏"回到 24 点
- [x] `npm test` 与 `vite build` 通过

## Comments

> *This was generated by AI during triage.*

- 2026-08-29 triage：实现在主干（commit 23f78dd）：`buildDetailBodyHtml` 字段序为 厂商 → 官网 → api_key → 接口规范 → 端点 URL → 标签 → 备注，缺字段照旧省略；`MASKED_API_KEY` 为 24 个 `•`（detailView.test.js 有断言）。复核 vitest 84/84、`vite build` 通过。`Status` 补记为 done，验收项勾选。
